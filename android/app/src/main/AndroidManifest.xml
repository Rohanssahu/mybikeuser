<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.CAMERA" />
 <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Photo selection uses Android's system Photo Picker. Explicitly remove
         broad media/storage permissions, including any added by libraries. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
<uses-permission android:name="android.permission.CALL_PHONE" />

<queries>
    <intent>
        <action android:name="android.intent.action.VIEW" />
        <data android:scheme="upi" />
    </intent>
</queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">
             <meta-data
     android:name="com.google.android.geo.API_KEY"
     android:value="AIzaSyD-wpc72_cdZesSpttpE2tXHbqlpp84JJA"/>

     

<meta-data
    android:name="com.google.firebase.messaging.default_notification_icon"
    android:resource="@drawable/ic_notification" />

<meta-data
    android:name="com.google.firebase.messaging.default_notification_color"
    android:resource="@color/notification_color"
    tools:replace="android:resource" />

    <meta-data android:name="com.dieam.reactnativepushnotification.notification_foreground" android:value="false" />
    <meta-data android:name="com.dieam.reactnativepushnotification.notification_channel_name" android:value="..." />
    <meta-data android:name="com.dieam.reactnativepushnotification.notification_channel_description" android:value="..." />
      <meta-data  
            android:name="com.dieam.reactnativepushnotification.notification_color"
            android:resource="@color/white"/> 
                <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value=""/>

             <receiver android:name="com.dieam.reactnativepushnotification.modules.RNPushNotificationActions" />
        <receiver android:name="com.dieam.reactnativepushnotification.modules.RNPushNotificationPublisher" />
      
       <receiver 
            android:name="com.dieam.reactnativepushnotification.modules.RNPushNotificationBootEventReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Firebase Cloud Messaging is handled by @react-native-firebase/messaging's
             ReactNativeFirebaseMessagingService, which the library contributes to the merged
             manifest and which index.js already programs against (setBackgroundMessageHandler,
             onMessage, onNotificationOpenedApp, getInitialNotification).

             RNPushNotificationListenerService must NOT be registered here. It is the first
             MESSAGING_EVENT service in the merged manifest, so it used to receive onNewToken, and
             its implementation calls
             ReactApplication.getReactNativeHost().getReactInstanceManager().createReactContextInBackground().
             react-native-push-notification 8.1.1 predates the New Architecture: on a bridgeless
             app getCurrentReactContext() is always null, so that call built a SECOND, legacy-bridge
             React instance next to the running bridgeless one. The legacy instance creates its
             Hermes runtime without a microtask queue, while the JS bundle sees
             enableBridgelessArchitecture() == true and installs the queueMicrotask-based
             setImmediate shim, so the first callback crashed the process with
             "Could not enqueue microtask because they are disabled in this runtime".
             The FCM token refresh fires this on the first launch after install.

             com.google.firebase.messaging.FirebaseMessagingService was also declared here. That is
             the SDK base class, it does nothing on its own, and declaring it would shadow the
             react-native-firebase service once the entry above is gone.

             react-native-push-notification is still used from JS for displaying local
             notifications (createChannel / localNotification); only its FCM entry point is removed. -->
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
